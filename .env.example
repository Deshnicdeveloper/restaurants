# Prisma Postgres (pooled URL, often starts with prisma+postgres://)
DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=REPLACE_ME"

# Direct database URL for migrations/db push (usually postgres://...)
DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require"

# Optional bookkeeping
PRISMA_POSTGRES_DB_NAME="prisma-postgres-aero-candle"
PRISMA_POSTGRES_ID="cmner7e4q0dpi1rf74bct439y"
NEXTAUTH_SECRET="replace-with-random-secret"
NEXTAUTH_URL="http://localhost:3000"

ADMIN_EMAIL="admin@restaurant.local"
ADMIN_PASSWORD="supersecurepassword"

PUSHER_APP_ID=""
PUSHER_KEY=""
PUSHER_SECRET=""
PUSHER_CLUSTER=""
NEXT_PUBLIC_PUSHER_KEY=""
NEXT_PUBLIC_PUSHER_CLUSTER=""

# Optional: Pusher Beams web push credentials (different from Pusher Channels above)
PUSHER_BEAMS_INSTANCE_ID=""
PUSHER_BEAMS_PRIMARY_KEY=""

NEXT_PUBLIC_APP_URL="http://localhost:3000"
